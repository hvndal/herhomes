<?xml version="1.0" encoding="UTF-8"?>
<!--
  Her Homes Co. — one page, one <url> entry. herhomesco.example is a
  placeholder (RFC 2606) — replace with the real domain once one exists.

  Everything on this site lives at this one URL as same-page anchors
  (#styles, #pricing, #founder, #book, etc.) rather than separate pages,
  so there is nothing else that belongs in this file: an anchor is not a
  distinct crawlable document, and a sitemap should only ever list real,
  indexable, canonical URLs. The moment separate pages exist (say, a
  dedicated /services/deep-cleaning or a blog), add one <url> block per
  new canonical page here — never for a page that is noindex'd, redirected,
  or canonicalised to somewhere else.
-->
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://herhomesco.example/</loc>
    <lastmod>2026-08-28</lastmod>
    <changefreq>monthly</changefreq>
    <priority>1.0</priority>
  </url>
</urlset>
